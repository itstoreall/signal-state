// import { firstSignal } from '../signals';
// import { setBacicFns } from '../utils';

// const first = () => ({ ...setBacicFns(firstSignal) });

// // ------ counter:

// const counter = {
//   counter: {
//     run: () => firstCounter,
//     stop: () => clearInterval(firstCounter)
//   }
// };

// const firstCounter = setInterval(() => {
//   firstSignal.value = { val: firstSignal.value.val + 1 };
// }, 1000);

// export default first;

export default {};
