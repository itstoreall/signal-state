// import { effect } from '@preact/signals';
// import { useGlobalContext } from '../hooks/useGlobalContext';

const Two = () => {
  console.log(2, 'Two');

  return <div>{`Two: `}</div>;
};

export default Two;
